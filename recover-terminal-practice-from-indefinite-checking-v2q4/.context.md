Fixed on branches fix/terminal-assessment-bounded-failure / pi-agent-fe7d68e8-d7d7-411 at eb242f1. Provider failure, non-final/empty review, or 30-second timeout now persists generic terminal-feedback-recorded and leaves Checking; there are zero workflow-level automatic external retries after the provider adapter’s own bounded retries. Feedback makes the old request stale, preventing read/restart/late-result call storms; a new command is the explicit retry. Added rejecting/hung Coach and Main Tutor, restart/read, rerun, and late-acceptance tests plus ADR 0020. Engine tests/check/build, workbook check, and calculator tests passed. Root eval and visual checks have documented unrelated existing failures. Diff independently inspected.
