Give the devcontainer a Linux-only node_modules volume or equivalent reliable isolation. A bind-mounted macOS node_modules caused node-pty and Rollup native optional dependency failures during the baseline work. Verify fresh and warm container runs.