Decision (Matt, interview): full confetti appears only when a lesson is completed. Replace the hand-built 24-DOM-particle CSS effect with canvas-confetti. Preserve one-shot triggering, pointer-inert rendering, cleanup, and reduced-motion support. Celebration should deliberately break out of the workbook palette rather than match it: begin with canvas-confetti’s lively saturated default colours and tune the burst visually in-browser. Accepted reflections should use restrained inline success feedback and must not trigger full-screen confetti.
