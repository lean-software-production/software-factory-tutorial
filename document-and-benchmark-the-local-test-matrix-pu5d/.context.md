Document fast versus complete commands, engine versus workbook evals, visual approval procedure, prerequisites, expected warm/cold runtime and token cost. Run the complete matrix from a clean local checkout/devcontainer and record timings.