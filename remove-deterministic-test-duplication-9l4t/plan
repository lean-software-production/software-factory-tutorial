Implement after the UX script merge:
- add engine build:typescript = rm -rf dist && tsc -p tsconfig.json;
- build = build:typescript + build:web:workbook;
- test:fast drops only redundant tsc --noEmit, retaining tsconfig.check strict superset, evaluator check, Vitest, one Vite build, and smoke;
- check = build:typescript + test:fast + terminal-image readiness;
- prepublishOnly delegates once to check;
- CI removes its separate build step and invokes check once;
- update exact command contracts/tests and add tsconfig.check superset regression;
- remove back-to-back check/test:fast guidance from docs/README while preserving check as compatibility alias;
- verify focused contracts, root test:fast, then engine check if Docker is available.
