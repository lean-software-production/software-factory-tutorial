After the UX package-script change lands, remove low-value deterministic duplication without reducing coverage: stop docs from running check then its exact test:fast alias; remove engine tsc --noEmit only if tsconfig.check remains a strict source/test/web superset; reorganize CI/package commands so engine emit build and web build are each performed once while retaining lint, strict type checks, full Vitest, browser smoke, workbook validation as applicable, and terminal-image build/readiness. Update command-contract and onboarding regression tests plus docs.