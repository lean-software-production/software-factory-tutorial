Use one shared, full-width feedback/status bar welded beneath both the editor and terminal work surfaces. This replaces any terminal-specific speech-bubble treatment and makes running, reviewing, updating, actionable feedback, temporary failure, and success visually consistent.

Likely areas: web-workbook/src/workbook-ui.tsx, terminal-coaching-display.ts, styles.css, activity-band.css, and a small shared PracticeFeedbackBar component if that reduces duplication.

Acceptance:
- The bar exactly matches the width of its editor/terminal surface and shares the border, corner, spacing, typography, colour, spinner, ARIA, and reduced-motion treatment.
- The work surface loses its lower radius/border where the bar is attached; the bar owns the lower corners.
- Status changes do not shift into a floating/right-aligned bubble or obscure the work surface.
- Existing actionable feedback can remain visible with a subtle updating indicator.
- Desktop, expanded activity-band, and narrow viewport layouts remain usable.
- Component/layout tests and approved visual baselines cover both editor and terminal states.