Implement the approved real-curriculum scenarios, deterministic gates, judge inputs, isolated learner sessions, separate reports, privacy constraints, command help and regression tests. Expose root-owned eval:workbook and eval:workbook:release commands.