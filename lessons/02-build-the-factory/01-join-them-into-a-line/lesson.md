---
durationMinutes: 40
outcomes:
  - Move the Part 1 scripts and prompts into a named refactor line.
  - Write shared success criteria that both doer and validator receive as input.
  - Update the station prompts and scripts so they use the shared criteria.
  - Create a run script that records a baseline, runs the doer, validates, and pauses after each iteration.
blocks:
  - key-concept
  - implementation-order
  - checks
  - pressure-test
---

# Join them into an assembly line

Give the two agents a boundary, a fixed order, and criteria that outlive a single turn.
