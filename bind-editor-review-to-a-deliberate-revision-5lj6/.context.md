Replace the 750 ms composition-time assessment lifecycle with an explicit Check my work action and Cmd/Ctrl+Enter. Typing and pauses save/edit locally but never create attempts. Preserve the one-time seeded-draft check needed for the authored spec, but do not repeatedly auto-submit learner prose. Bind every request and visible result to the exact submitted text/version; while a replacement review runs, do not present prior feedback as if it applies to unreviewed edits. Add regression coverage reproducing the 38-attempt prompt session and proving one attempt per explicit check. Record the durable decision in a new tutorial-engine ADR and index it.