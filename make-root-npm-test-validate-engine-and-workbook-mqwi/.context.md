Make root npm test the comprehensive local release gate for both tutorial-engine and the authored tutorial. Keep npm run test:fast deterministic and model-free. Root orchestration delegates engine ownership to --workspace=tutorial-engine. Full tests include canonical visual approvals plus separately reported live engine and authored-workbook evals. No CI dependency.

Implementation complete through 4654e74: terminal/visual fixes, engine eval ownership/preflight/release, authored runner, root aggregate matrix, docs/benchmarks. Deterministic test:fast passes (cold 128.57s, warm 109.33-111.03s on macOS). Only remaining child is paid live review, manually blocked by local Docker daemon timeout before any paid call/report.
