Final definition-of-done play-test on the fully integrated branch. In one fresh session verify: no editor review calls while composing; one auditable review per explicit check; forgiving equivalent-language prompt acceptance; accepted spec/prompt/loop remain read-only in history; all five passes and commits complete; the learner can play and quit Tetris; reflection accepts one substantive answer; ordinary chat and summaries show thinking consistently; the composer remains responsive after terminal history; normalized transcript preserves Unicode; and provider failure/retry recovery remains clear. Record event IDs, review latency, keystroke latency, attempt counts, and pass/fail findings in this Yak.