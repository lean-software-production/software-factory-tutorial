Diagnostic work only: reproduce the sluggish composer with a production web build and a Part-2-sized timeline including completed terminal history. Measure median/p95 keystroke-to-paint and attribute time among TimelineThread rerender, Markdown/highlight, terminal history, duplicate input handlers, textarea scrollHeight layout, Mermaid, xterm, and SSE state fetches. Run controlled A/B spikes without committing a product fix. Publish the reproducible harness and numbers that define the parent Yak's implementation and acceptance threshold.