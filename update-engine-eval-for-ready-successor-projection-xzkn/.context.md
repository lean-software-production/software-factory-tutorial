Root npm check currently fails because evals/test/v2-driver.test.ts still expects only the active orientation block after introduction. Since commit 2b97f4c intentionally exposes the ready successor's authored block data to preload the practice surface, public chapters contain orientation plus editor-practice. Update the evaluator regression to assert the current rendered/ready contract without weakening privacy or progression checks. Also ensure test commands exclude unrelated .worktrees copies.