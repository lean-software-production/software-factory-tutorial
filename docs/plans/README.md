# Historical plans

This directory preserves older implementation plans and design sketches for the repository.

They are historical records, not current navigation. Paths and commands inside these files may predate the split that moved learner-authored content under `tutorial/`. Prefer current documentation in the root `README.md`, `tutorial/README.md`, `AGENTS.md`, and `tutorial-engine/README.md` before following any path named here.
