## Goal

Run the authoritative deterministic and visual verification after review findings are resolved.

## Commands

From the isolated worktree:

```sh
npm run --workspace=tutorial-engine test -- \
  test/workbook-attempts.test.ts \
  test/workbook-block-progression.test.ts \
  test/activity-band.test.tsx \
  test/workbook-ui.test.tsx
npm run test:engine:fast
npm run test:fast
npm run check:visual
```

## Acceptance

- All focused, engine-fast, and root-fast deterministic gates pass.
- The two original regressions have direct passing tests.
- Inspect visual received output for accepted-live and completed-history states. Do not approve screenshots automatically; leave deliberate approval to Matt in the canonical devcontainer.
- Do not run paid live evals, Docker-backed release lanes, or root `npm run test`.
- Record command outcomes, changed screenshot paths, branch, and final commit in the handoff.
