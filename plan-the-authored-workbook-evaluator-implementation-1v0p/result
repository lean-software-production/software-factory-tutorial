Detailed test-first plan complete. Each scenario uses a fresh honest curriculum slice rather than lesson-jump completion; scripted learner uses only public HTTP/WebSocket protocols; reports exclude raw internal events/private rubrics; Lesson 001 uses real nested Pi and later journeys use allowlisted deterministic stubs. Ten-step plan covers shared trace/driver, slices, stubs, seeds, scenarios/gates, judge/reports, preflight, runner/CLI, docs/live review. Agent 5688565e-bde6-4f1.
