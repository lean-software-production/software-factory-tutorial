Fix the editor feedback/loading-state behaviour observed in the play-test. First reproduce the display glitch with controlled delayed and out-of-order review responses, then trace the server/SSE/POST/UI state boundary before changing it.

Desired behaviour:
- Reviews start only after editing has gone quiet; continuous typing does not review partial drafts.
- Existing actionable feedback remains fully visible while a newer review is pending.
- A subtle spinner/updating indicator appears beside the retained feedback rather than replacing it with generic loading copy.
- Only the latest editor revision may update feedback or unlock the block; stale and out-of-order results cannot win.
- New feedback replaces old feedback atomically when complete.
- Transport/provider failure retains useful feedback and shows a non-destructive retry status.
- CodeMirror focus, cursor, and current draft survive state refreshes.

Likely areas: web-workbook/src/workbook-ui.tsx, the editor checkpoint/projection in workflow.ts and public-contract.ts, plus workbook-ui and workbook-server tests.

Acceptance tests should use deferred responses to cover typing during review, old-after-new completion, failure with prior feedback, atomic replacement, quiet debounce, and focus/cursor preservation.