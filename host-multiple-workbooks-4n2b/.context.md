I want a workbook that explains how to use the tutor, a meta-tutorial. I think it can also act as the test fixture since it will be showcasing all the features.

I want a workbook that does ralph loops.

I guess the engine needs to offer you a choice of which workbook?

Maybe workbooks/parts can be composed of lessons that live in their own library folder - composed by path not ID.

Learning outcomes need to be presented differently, off to the side.

Scrollytelling research needs bringing in.

Code architecture needs a review.

Terminal scrollytelling is decent but needs to start earlier. Maybe include some instructional text too?

Need to test the editor's scrollytelling

Editor needs syntax highlighting

Terminal live feedback is twitchy and awkward.

Chatting with the tutor needs exploring - if I'm looking at an older lesson should it insert at the bottom of the currently-visible block? That feels more right but it's also probably complicated.

Scroll position when chat messsages are written to the timeline/canvas is off

There's a small glitch/jump as you scroll

the terminal-practice block is too slow to load, it's doing some kind of LLM call before we get to see/touch the terminal

Ideally a wysiwyg editor for the workbook to add/delete blocks, edit markdown.

Do we really need to cd calculator all the time? Can we just build our factory inside the calculator folder?

Generally the copy has a robotic feel

Need to actually play-test the thing and make sure the tutor prompts are right
