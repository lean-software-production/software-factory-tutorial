{
  "name": "validation-loops-tutorial",
  "private": true,
  "version": "0.1.0",
  "description": "A browser-led tutorial for building software validation loops.",
  "type": "module",
  "engines": {
    "node": ">=24.2.0 <25",
    "npm": ">=11.0.0"
  },
  "workspaces": [
    "calculator",
    "tutorial-engine"
  ],
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "tutorial": "node scripts/tutorial.mjs",
    "test:onboarding": "node --test test/onboarding.test.mjs",
    "test:eval": "vitest run evals/test --root .",
    "check:eval": "tsc -p evals/tsconfig.json",
    "eval": "npm run --workspace=tutorial-engine build && tsx evals/run.ts",
    "check": "npm run test:onboarding && npm run check:eval && npm run test:eval && npm run --workspace=tutorial-engine check && npm run --workspace=calculator test"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.83.0"
  }
}
