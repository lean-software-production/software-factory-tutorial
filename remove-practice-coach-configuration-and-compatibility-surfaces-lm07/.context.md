After direct Main Tutor terminal review is working, delete the obsolete Practice Coach implementation and configuration rather than leaving a dormant compatibility adapter.

Remove practice-coach runtime code/tests, PRACTICE_COACH_MODEL resolution and preflight, server dependency injection, fake coach queues, setup/onboarding configuration, and current docs/fixtures that describe a second model-backed role. Keep only the minimum legacy event parsing/privacy projection needed to reopen old sessions.

Update tutor/server/model-preflight tests, eval fixtures, setup/onboarding tests, and current README documentation. Historical accepted/superseded ADRs and plans remain historical.

Acceptance:
- Repository search outside historical ADRs/plans finds no live PracticeCoach type, PRACTICE_COACH_MODEL configuration, or new handoff writer.
- Startup preflights one Main Tutor role.
- Tests/evals use direct Main Tutor terminal decisions.
- Old event logs with handoffs still project safely.
- Root and tutorial-engine checks pass.