Outcome: one stateless, workspace-aware Main Tutor handles conversation, editor/reflection review, terminal review and summaries. Every operation reconstructs ADR 0006 history from the canonical event log, including authored content as prior assistant speech, active-block detail, block summaries within the active lesson and lesson summaries for completed lessons.

ADR 0026 is authoritative. Active practice blocks expose only workspace-bounded list_files/read_file with ordinary traversal/static-symlink containment. Bash-finished terminal evidence is a bounded private snapshot stored directly in the versioned event log. Reviews have accepted/feedback outcomes only. Every Tutor operation gets three automatic provider attempts; exhaustion creates a process-local fatal error, blocks progression, and requires fixing the provider and restarting. There is no manual retry, pending-effect restart recovery, separate evidence repository, Practice Coach, legacy-session compatibility, working outcome, adversarial symlink-race machinery, or faster terminal model.

Current sessions declare an explicit event-log format version; unsupported/missing versions fail clearly. Required summaries succeed before completion is recorded. Live evals must pass one clean full run. Visual coverage uses two canonical desktop/narrow composites. Latency evidence belongs in Yaks, not a committed benchmark note.

Done when the implementation matches ADR 0026, all deterministic/root/visual gates pass, every live eval passes one clean provider-qualified --all --yes run, privacy searches pass, and final whole-branch review is clean.
