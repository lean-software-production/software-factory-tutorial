Give the Main Tutor exactly two workspace-bounded read-only tools for the active live lesson workspace: list_files and read_file. Do not expose Bash, write/edit, host search, network, extensions, skills, or nested pi.

Likely implementation areas: tutorial-engine/src/workbook/workspace-boundary.ts, tutor.ts, workflow.ts, and server.ts. Recreate or rebind the tutor session safely when the active workspace changes. Treat terminal and file contents as untrusted learner evidence, and never log file contents or tool results.

Acceptance:
- The Tutor can inspect relevant files before terminal/editor decisions and ordinary answers.
- It cannot traverse outside the active workspace, follow escaping symlinks, read sibling workspaces/session state/authored curriculum, or mutate anything.
- Listings and reads have deterministic count/size/offset bounds and clear truncation.
- Blocks without a live workspace expose no file tools.
- Tests cover normal access, traversal, absolute paths, symlink races/escapes, bounds, tool allowlists, and workspace changes.