Current V2 evaluator reads raw workbook/events.jsonl into trace.events and sends those citations to the judge. Durable terminal lifecycle records include private commands, evidence refs and Coach handoffs; regex sentinel checks are not a public projection. Build a structurally allowlisted public judge trace while retaining any internal records needed only for deterministic gates, and test that terminal lifecycle/private handoff records cannot enter judge prompts or reports.