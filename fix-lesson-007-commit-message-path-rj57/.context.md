Lesson 007 writes `.tmp/commit-message.txt` but sets `message="$PWD/commit-message.txt"`; the subshell therefore reads a nonexistent path. Change authored lesson and spec to `$PWD/.tmp/commit-message.txt`, preserving the generated-state-under-.tmp rule. The authored evaluator completed-factory seed already uses the corrected path and must stay aligned.
