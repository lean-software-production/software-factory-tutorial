Plan complete. Move synthetic V2 evaluator wholesale to tutorial-engine/evals with history; preserve six v2 IDs and privacy/audit behavior; workspace owns check:eval, test:eval, eval and eval:release; root forwards eval:engine with argument passthrough; keep temporary root eval alias; reports move to tutorial-engine/evals/reports with explicit owner/suite/schema marker. Add bounded --release, fail-fast Docker/model/judge preflight, and separate docs. Do not share infrastructure with authored-workbook eval yet. Agent 981eec2d-2e54-4f7.
