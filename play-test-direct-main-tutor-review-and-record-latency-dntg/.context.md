Final verification for ADR 0026 after implementation alignment completes.

Run current root and tutorial-engine checks, onboarding, calculator tests, deterministic workbook UX, versioned current-session lifecycle tests, privacy searches, and canonical OrbStack visual review using two desktop/narrow composites. Run one clean provider-qualified `npm run eval -- --all --yes`; every live scenario must pass. No legacy-session or ralph.sh-specific replay is required.

Record the existing latency benchmark in this Yak: TUTOR_MODEL openai-codex/gpt-5.6-luna, n=10 direct terminal reviews, median command-finished-to-decision 2.878s, nearest-rank p95 6.470s, retry rate 0%, outcomes 4 accepted / 6 feedback, contradictions 0. This does not justify a faster terminal model.

Done when all gates pass once on the aligned branch and final review confirms fatal provider handling, feedback-bar stability, privacy and no obsolete compatibility/retry surfaces.
