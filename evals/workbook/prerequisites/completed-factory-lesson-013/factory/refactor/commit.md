Write the git commit message for the calculator change that just passed validation.

Use the diff and validator findings appended below. The message must be a subject line under 72 characters, a blank line, then two or three lines saying what changed and which success criteria it moved.

Emit only the commit message. Do not include a preamble, code fence, or explanation.
