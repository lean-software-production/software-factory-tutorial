---
type: terminal-practice
tutor: |-
  Do not reveal an exact command for this block.

  Success: the learner creates .tmp/evaluator-clue.txt with the text "clue block complete" and
  prints the file back. If they ask for help, give one small shell clue at a time: make the .tmp
  directory, write the file, then read it back.
---
## Use the clues

Create `.tmp/evaluator-clue.txt` containing `clue block complete`.
Then print it back with a command that reads the file.

Clues:

- keep the file under `.tmp/`;
- create the directory before writing the file;
- use one shell command to write the text and another to display it.
