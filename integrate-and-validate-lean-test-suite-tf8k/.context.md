Review merged diffs for contract/privacy regressions. Run focused suites after each merge, then npm run test:fast, canonical command-contract tests, deterministic UX analyzer as practical, and model-free release selection/report tests. Do not launch paid release evals unless needed to validate behavior changed by this task; report any remaining live-validation need honestly.