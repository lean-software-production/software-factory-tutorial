Replace the terminal-only Practice Coach handoff path with direct Main Tutor review of completed terminal evidence.

At command completion, retain immutable command-local evidence (command, interactions, exit status) and a labelled bounded snapshot of the available terminal transcript. Send that evidence directly to the Main Tutor, which may inspect the active workspace with list_files/read_file before giving feedback or accepting. Continue recognizing old terminal-coach-handoff-recorded events only so legacy sessions can replay; no new handoff events should be written.

A transient provider error, timeout, empty response, or invalid working result is infrastructure failure, not learner feedback. Preserve the attempt/evidence and retry review against the same evidence with a bounded total call budget. If automatic retry is exhausted, offer a review-only retry; never require the learner to rerun a successful command. Late results for superseded attempts must not win.

Instrument command-finished-to-decision latency, direct review duration, retry count, and outcome without logging commands, transcripts, paths, rubrics, or tool contents.

Likely areas: terminal.ts, terminal-evidence.ts, terminal-attempt-projector.ts, timeline.ts, workflow.ts, tutor.ts, public contracts, and server tests.

Acceptance:
- One finished command causes direct Main Tutor review and no Practice Coach call.
- The original successful bash ralph.sh evidence can be accepted after an injected review failure without another command submission.
- Wrong commands receive useful Main Tutor feedback.
- Full bounded transcript and command-local evidence remain private.
- Existing sessions containing coach handoffs/failures still load.
- Tests cover direct acceptance/feedback, retry and exhaustion, restart recovery, transcript truncation, stale results, and privacy.