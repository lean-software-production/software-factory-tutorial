# Historical engine plans

This directory preserves planning material for earlier tutorial-engine iterations.

They are historical records, not current navigation. Some paths inside them predate the split that moved learner-authored content under `tutorial/`, or describe engine experiments that have since changed. Use `tutorial-engine/README.md` and `tutorial-engine/docs/adr/` for current engine orientation.
