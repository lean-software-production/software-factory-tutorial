---
type: terminal-practice
outcome: "Run three harmless status commands and read the Main Tutor feedback after each one."
tutor: "Private terminal rubric for the video recorder: return the queued deterministic Main Tutor outcome; do not call a model."
---
## Terminal check

Type the commands yourself instead of using a paste-only shortcut. The fixture shell is fake: it echoes deterministic output and records authoritative workbook command markers, but it never runs the commands.

```sh command
printf 'small state ready'
```
