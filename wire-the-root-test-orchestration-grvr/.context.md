After engine visual/eval ownership and workbook evals exist, wire root npm test as the complete local gate and npm run test:fast as the deterministic loop. test:engine delegates to --workspace=tutorial-engine scripts; test:workbook remains root-owned. Add package-script contract tests and clear preflight failures for Docker, credentials and model configuration.