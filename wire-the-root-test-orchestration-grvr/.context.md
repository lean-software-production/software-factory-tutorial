After engine visual/eval ownership and workbook evals exist, wire root npm test as the complete local gate and npm run test:fast as the deterministic loop. test:engine delegates to --workspace=tutorial-engine scripts; test:workbook remains root-owned. Add package-script contract tests and clear preflight failures for Docker, credentials and model configuration.

Live evaluator dependency integrated c1de17e and deterministic checks pass. Paid primer/release smoke remains externally blocked: Docker daemon/image preflight times out before model calls; no reports. Root orchestration plan completed; implementation agent 5024df11-93b3-402 active. Bare authored --release will derive fixed catalog budget so root contract command is executable.
