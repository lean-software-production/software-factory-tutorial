Improved CLI observability in a4cbc5b + 0659a10. `test:workbook-ux` now prints five explicit stages before work begins, routes the local server line as indented detail, reports all 12 checkpoint names/counts, warns that WebM decode may take several minutes, states AI timeout/skip/unavailability, announces report writing, and ends with deterministic/exit verdict plus artifact paths. Programmatic API stays quiet unless given typed progress sink. Direct `:record` also reports progress. README includes sample. Verification: causal progress tests (AI/no-AI/unavailable/failure/headed), TypeScript, synthetic actual-WebM contract, and a real deterministic run passed in isolated implementation worktree; post-merge focused workbook UX tests 35/35 and TypeScript passed.
