Implemented and merged a linear checked-out-engine UX video factory. Stations: representative journey fixture -> real 1280x900 Playwright WebM -> Chromium-decoded deterministic motion analysis -> optional `pi -p -nt --no-session --thinking low` advisory review -> report.md/factory-result.json. The journey types in CodeMirror and xterm at small, mid-scroll, and full-width states; separately marker-labels actual scroll transitions and Tutor/Coach feedback arrival in all six states. Deterministic checks cover marker integrity, required motion, no-motion, oscillation, isolated jumps, confidence/texture, feedback safe region and elementsFromPoint occlusion. Synthetic actual-WebM contract proves deliberate oscillation/jump/no-motion and smooth scroll pass. Full engine check passed: 54 files / 491 tests, lint, TS/eval, build, browser smoke. Latest full factory deterministic verdict passed with no findings; video ~55s/3.2MiB and all feedback unoccluded. AI station timed out in the integrated run but remained advisory/unavailable and did not change exit; replaying identical `pi -p` evidence manually completed with no scoped issues. Artifacts: tutorial-engine/test/.tmp/workbook-factory/latest/{walkthrough.webm,walkthrough.json,analysis/motion.json,analysis/contact-sheet.png,report.md,factory-result.json}. Commands: factory:workbook, factory:workbook:deterministic, factory:workbook:record, test:video-analyser.
