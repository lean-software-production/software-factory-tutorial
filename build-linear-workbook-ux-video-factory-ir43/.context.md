Completed the internal naming cleanup in fca73f4: implementation moved from `tutorial-engine/test/workbook-factory/` to `tutorial-engine/test/workbook-ux/`; active TS identifiers use WorkbookUxTest naming; tmp root is `test/.tmp/workbook-ux`; machine result is `ux-test-result.json`; fixture-only UX-factory wording was removed. Public scripts remain `test:workbook-ux` and its `:deterministic`, `:record`, `:analyser` variants. Verification in isolated worktree: focused tests, TypeScript, actual-WebM analyser, real deterministic journey, and full engine check all passed. Post-merge verification: 28/28 focused tests and TypeScript passed; active grep found no stale workbook-factory naming.
