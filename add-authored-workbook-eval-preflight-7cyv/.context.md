Fail fast before reports/model sessions on Node/npm, explicit tutor/Coach/judge identities, Pi connectivity, Docker/image/container auth and forbidden private prompt logging. Make probes injectable and keep help/unit tests model-free. Print scenarios, models, calls and expected budget.

Agent 95af12aa-b428-49d produced candidate 54622c4. Reviews 569909f4-19bf-497, 31a98b71-ffee-49d, and a843ca7d-bff7-4ff are active. Initial inspection found production Object.freeze(process.env), global async process.env mutation, swallowed cleanup, and unbounded Docker probes.

All reviews complete. Additional required fixes: actual EVAL_JUDGE_COMMAND probe, unpaid Docker before paid calls, mandatory per-role scenario budgets (no zero/numeric ambiguity), production terminal helper reuse, remove dead mountProbeHostPath, exact PRACTICE_COACH_LOG_PROMPT=1 parity. Amendment intentionally waits for finalized engine terminal/preflight primitives to avoid duplicate Docker/auth code.

Engine preflight dependency integrated as 76ded82. Final authored-preflight implementation agent 767eb647-c624-446 active on current workbook-tutor; must preserve current scripts/seeds/contract and fix all three prior review sets.

Candidate 1fa16a8b0e29a07b92a0cb0a4f222a15dfb591b0 passes authored/full-engine/workbook/onboarding/calculator checks. Final independent reviews active: security/ordering cfc1ea10-7050-421; shared primitive/API/current integration 17a1d237-5858-431.

Reviews rejected candidate: bespoke READY mount did not exercise actual stub/config; token-per-call minimum unenforced; parser fabricated zero-cost scenarios; duplicated Docker ready logic; timeout/cleanup branch gaps. Current-base final-fix agent ceabe3e5-056c-474 active. Scenario agent steered to provide immutable conservative per-role expectedModelCalls.

Amended current-base-at-start candidate 04c23c5 replaces READY sentinel with actual command-stub fixture, enforces 2000 tokens/call, catalog-based budgets, production Docker-ready primitive, cleanup/timeout coverage. Final independent reviews active: security/ordering 9c2e9e71-4c72-4bb; API/stub/current integration e8ca56e6-e974-43d. Candidate predates Judge/report 4030d0c so integration must preserve those README docs.

Final reviews rejected 04c23c5. Remaining: fixture creation timeout races detached async work and leaks root; run API still accepts caller-supplied underdeclared scenarios rather than production catalog IDs; parser wrongly requires every role >=1 (valid primer may use zero Coach); review noted upcoming catalog object freeze mismatch (scenario amendment is now deep-freezing); README conflict with 4030d0c. Final integration should happen after scenarios land, import/use authoritative catalog at production execution boundary (test seam separate), allow nonnegative role counts with judge>=1/total>0, and include paid preflight probes themselves in budget in addition to scenario calls.
