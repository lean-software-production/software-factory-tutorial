Add a first-class --release selection containing the six existing engine scenarios exactly once, reject conflicting scope/repeat flags, and expose workspace/root eval:release commands with deterministic CLI tests.