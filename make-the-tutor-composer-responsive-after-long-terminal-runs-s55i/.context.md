Fix post-Ralph composer lag only after the production-Chromium diagnostic child establishes the cause. Expected candidates are isolating composer draft state so keystrokes do not rerender full Markdown/terminal history, removing duplicate onInput/onChange updates, memoizing stable history, and reducing forced scrollHeight layout. Apply only evidence-supported changes. Preserve timeline ordering, scroll reveal, terminal history, Mermaid, and feedback behavior. Done against the same before/after latency harness with a material full-Part-2 improvement and no fast-path regression.