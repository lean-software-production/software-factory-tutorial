## Goal

Keep completed editor and terminal evidence visible as read-only history beneath each authored block.

## Work

- Generalize the timeline history insertion path so each completed practice block renders its own historical surface beneath its authored record.
- Render one read-only editor seeded from the latest attempt-correlated accepted editor content snapshot event and `contenteditable="false"`.
- Render one frozen terminal seeded from the latest attempt-correlated accepted terminal transcript snapshot event.
- Ignore orphan or stale snapshot events and reconstruct both historical surfaces from the event log after restart.
- Keep completed success feedback attached to the corresponding historical surface.
- Ensure completion removes the live surface from `.current-activity-band`, cancels pending editor submissions, unregisters terminal insertion, and closes/releases live terminal transport without duplicate history.
- Historical surfaces must not autofocus, schedule review, publish local revisions, accept terminal input, or reconnect.
- Preserve the same result after browser/server restart.

## Constraints

Use existing editor/terminal visual language. Do not turn historical surfaces back into active block targets. Keep reflection rendering unchanged.

## Deliverable

Make completed-history component, workflow restart, and no-submission/reconnect tests pass. Commit implementation and focused test evidence.
