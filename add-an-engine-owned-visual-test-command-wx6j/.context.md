Completed by bb8a121, merged in acd4159. Engine owns `test:visual`; non-container host enters/reuses devcontainer, public command re-invokes and validates canonical marker inside, noncanonical containers refuse, direct path builds only web bundle then runs visual harness, and all args are rejected. Integrated result preserved newer eval/video scripts. Validation: 7 command tests, both TS checks, 20 onboarding tests, real fresh canonical run in 66s; no PNG changes. Agent eb293d20-05cd-4ab. Reviews 0fdaef66-027d-40d and ba1a0731-5fc9-45a confirmed the command itself; their root check-visual bypass finding is the explicitly separate now-unblocked “Remove the changed-files visual gate” task. Docker-backed devcontainer is the canonical renderer; Windows host shim support is not part of the current macOS workflow.
