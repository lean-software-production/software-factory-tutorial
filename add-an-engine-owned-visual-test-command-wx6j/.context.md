Add tutorial-engine workspace scripts that build as needed and run test/visual-affordances.mts inside the canonical devcontainer. The command must be independently runnable and reusable by the full engine test. Benchmark warm and cold runtime.