Goal: preserve the deliberate root npm test release-gate contract while removing redundant deterministic work and paid/model-backed repetition.

Approved scope:
- make workbook UX AI review opt-in rather than the default; retain the authoritative deterministic video analyzer for periodic/UI-heavy validation;
- retain all six synthetic engine scenario IDs for targeted diagnosis, but reduce the release profile to the locked-editor negative branch and one full successful transition journey whose composed deterministic gate preserves all positive-path assertions; reduce release Judges accordingly;
- keep all four authored curriculum slices, but make Judge participation scenario-specific: keep semantic Judges for primer, Lesson 001, and Lesson 013; skip the routine Judge for the mechanically gated Lessons 003-004 path; preserve closed report/latest schemas and honest lifecycle metadata for both judged and deterministic-only success;
- remove deterministic duplication: docs must not tell maintainers to run check and test:fast back-to-back, engine source must not be typechecked twice when tsconfig.check is the strict superset, and CI must not compile the same engine/web outputs twice while still proving an emit build, full deterministic checks, browser smoke, and terminal image readiness;
- preserve canonical visual validation, all four authored slices, root npm test semantics, privacy/security guarantees, and targeted diagnostic commands.

Out of scope: model/token metering or pricing work.

Workflow: independent implementation agents in isolated worktrees; focused tests and independent reviews before each early merge; final deterministic and release-contract validation after integration.