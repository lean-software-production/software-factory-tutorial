The calculator passes its tests, reveals intention, avoids duplication, and has few unnecessary
elements. The validator should cite the supplied test output, quality reports, and working diff for
each judgement.
