# Lesson 007 jump seed

This is the test-only starting contract for `--lesson 007`. Its `factory/` overlay represents the
line after lesson 006: the harness gathers labelled evidence, and the validator has no `bash` tool.
It replaces only a jump session's `factory/` before that session's initial Git commit; it never
changes the authored tutorial or normal learner sessions.

`.tmp/quality-before.txt` belongs here because lesson 006 leaves the validator's recorded baseline
in place. `run.sh` overwrites it on its next iteration.
