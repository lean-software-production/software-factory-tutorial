---
type: editor-practice
outcome: Read and optionally refine the Tetris product specification.
path: spec.md
tutor: |-
  Accept when `spec.md` describes a terminal Tetris game with a display that fits within 24 rows.

  Look for:
  - a clear product name or description identifying this as Tetris;
  - a start command such as `npm start`;
  - a row-count constraint keeping the display within 24 terminal rows;
  - permission or guidance to install packages.

  The spec is already seeded in the workspace. The learner may read it as-is, or make small
  refinements. Do not require invention; accept the seeded spec without changes. Do not ask the
  learner to add tests, architecture, controls, rule details, or a validator.
---

## Read your spec

Your workspace already contains `spec.md` — the product specification the worker agent will build
toward. Open it and read it.

You may leave it exactly as it is, or make small changes if you want to adjust the target. Keep it
brief. The spec deliberately leaves architecture, controls, and visual style to the worker: that is
what gives the agent room to make decisions.
