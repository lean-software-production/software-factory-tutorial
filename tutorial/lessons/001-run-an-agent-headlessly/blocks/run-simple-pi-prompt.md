---
type: terminal-practice
tutor: |-
  Have the learner run the displayed command in the embedded terminal.

  Success: Pi prints the answer and exits. The model's exact wording does not matter.

  If the learner needs help, point out that -p provides the job/prompt. After responding, Pi exits
  rather than opening a conversation.
---

## Run a simple Pi prompt

Run this command:

```sh command
pi -p "What is the capital of France?"
```

The `-p` option supplies a prompt. Pi should print the response and then exit.
